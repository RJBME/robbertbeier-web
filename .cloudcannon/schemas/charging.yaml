collections_config:
  charging:
    path: _data/charging.yml
    parser: yaml
    name: Charging Sessions
    schemas:
      default:
        path: .cloudcannon/schemas/charging.yaml  # <--- DOUBLE CHECK THIS PATH