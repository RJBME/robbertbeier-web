collections_config:
  charging:
    path: _charging
    name: Charging Sessions
    icon: ev_station
    schemas:
      default:
        path: .cloudcannon/schemas/charging.yaml
    _editables:
      content: false
    add_options:
      - name: Add Charging Session
        schema: default